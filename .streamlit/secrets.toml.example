# Streamlit Community Cloud secrets template.
# On Streamlit Cloud, paste this in: App Settings → Secrets (TOML format).
# Locally, copy this file to .streamlit/secrets.toml — it's gitignored.

OPENAI_API_KEY = "sk-your-key-here"
